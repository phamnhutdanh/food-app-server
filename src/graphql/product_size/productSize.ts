import { productSizeMutation } from "./productSizeMutation";
import { productSizeQuery } from "./productSizeQuery";
import { productSizeTypeDef } from "./productSizeTypeDef";
import { productSizeResolver } from "./productSizeResolver";

export const ProductSize = {
  productSizeTypeDef,
  productSizeQuery,
  productSizeMutation,
  productSizeResolver,
};
